import { AppConfig } from './app-config';

const appConfigExtended: Partial<AppConfig> = {
  port: 45349,
  name: 'DirectFN Pro11',
  bundleId: 'com.directfn.pro11.retail',
};

export default appConfigExtended;
